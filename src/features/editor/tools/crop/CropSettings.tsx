export default function CropSettings() {
  return (
    <div className="p-4">
      <h4 className="text-sm font-semibold">Crop</h4>
      <p className="text-xs text-gray-400 mt-2">Click and drag on the canvas to define a crop area.</p>
    </div>
  );
}